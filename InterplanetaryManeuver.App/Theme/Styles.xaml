<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <FontFamily x:Key="Font.Ui">Segoe UI Variable Display</FontFamily>
    <FontFamily x:Key="Font.Mono">Cascadia Mono</FontFamily>

    <Style TargetType="{x:Type Window}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Ui}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text0}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text0}" />
    </Style>

    <Style x:Key="Button.Primary"
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Padding" Value="16,11" />
        <Setter Property="Foreground" Value="#F6FBFF" />
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#3C5E74" Offset="0" />
                    <GradientStop Color="#27455A" Offset="0.6" />
                    <GradientStop Color="#1B3243" Offset="1" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="#6FE8FF" />
        <Setter Property="BorderThickness" Value="1.2" />
    </Style>

    <Style TargetType="{x:Type Border}" x:Key="Panel">
        <Setter Property="Background" Value="{StaticResource Brush.PanelBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Line}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="18" />
        <Setter Property="Padding" Value="18" />
    </Style>

</ResourceDictionary>
